/**
 * @type {import('tinacms').TinaField}
 */

import { TinaField } from "tinacms";
let image : TinaField = {
    type: "image",
    name: "image",
    label: "image",
    required: true,
};
export default image;